# PLACEHOLDER — replace this file

This is scaffolding, not real instructions. It ships as a placeholder so the
provisioning plumbing can be tested before the actual content exists.

Replace everything in this file with the learn-context CLAUDE.md: Claude as a
teacher who guides and instructs the student rather than writing the code.

Applies to every project under `~/Dev/learn/`, including git repos — CLAUDE.md
discovery walks up across the repo boundary.

## Keep this section when you rewrite the file

Skills for both contexts are installed together in `~/.claude/skills/` and are
all visible in every session, because skill discovery — unlike CLAUDE.md
discovery — stops at the git repo root and cannot reach a shared parent
directory.

Use the `learn-*` skills. Ignore the `build-*` skills; they belong to the
vibe-coding context and do not apply here.
